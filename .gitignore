# Dependencies
node_modules/

# Build output
dist/
build/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
*.code-workspace

# Root lockfile only (do NOT block /web/pnpm-lock.yaml)
#/pnpm-lock.yaml       <-- uncomment if a root lockfile appears; keep the leading slash
/pnpm-lock.yaml

# Env at root (safe even if you don’t have any here)
.env
.env.*
